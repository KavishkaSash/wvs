import { NextPage } from "next";
import ReWeightView from "../components/ReWieghtView";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return (
    <div>
      <ReWeightView />
    </div>
  );
};

export default Page;
